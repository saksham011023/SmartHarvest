services:
  - type: web
    name: app
    env: python
    build_command: |
      apt-get update
      apt-get install -y gfortran build-essential  # Install gfortran and any other necessary packages
      pip install -r requirements.txt
    start_command: gunicorn app:app

